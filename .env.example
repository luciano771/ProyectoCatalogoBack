# Variables de entorno del backend (copiar a .env y completar)
# Cargadas por dotenv y leídas en src/config/env.ts

NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://usuario:password@localhost:5433/proyecto_catalogo?schema=public
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=cambiar_por_valor_seguro_en_produccion
JWT_EXPIRES_IN=7d
